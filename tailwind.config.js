module.exports = {
  content: [
    "./build/*.html",        // Your HTML files in the build folder
    "./src/**/*.js",         // All JS files inside src folder (including subfolders)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
s